globals() 

Return the dictionary implementing the current module namespace. 
For code within functions, this is set when the function is defined and remains the same regardless of where the function is caled. 

All global names (variable name, function-name) are stored in global namespace (dictionary). 